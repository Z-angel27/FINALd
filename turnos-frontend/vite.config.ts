import { defineConfig } from 'vite'

// Minimal Vite config for the frontend. The @vitejs/plugin-react plugin
// was removed to avoid an ESM loading issue in some environments. You can
// re-add it later if your environment supports ESM configs.
export default defineConfig({})
